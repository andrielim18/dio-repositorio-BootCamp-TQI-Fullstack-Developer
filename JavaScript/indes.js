// fazer comentários
//criar variáveis: variáveis var são mutáveis
//constantes: const - são imutáveis e como boa prática sempre usar letras maiusculas
var x = 1;
var y = 2;
var z = x+y;
const PRECO = 3;
var desconto =0.2;
total = PRECO-dessconto;

//FUNÇÕES

function soma(a,b){
    //console.log(a+b) //usado para ver se algo está correto, sem precisar retornar
    return a+b; //usado quando precisa retornar o valor da função
}

soma(3,5);
// USANDO O CONSOLE NA WEB

